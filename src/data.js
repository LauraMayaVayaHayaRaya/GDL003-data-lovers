// esta es una función de ejemplo
// puedes ver como agregamos la función a nuestro objeto global window

const showData = () => {
  
  return 'example';
};

const filtrado =() => {
  return 'filtrado';
};

window.filtrado = {
  filtrado,
  example
};
